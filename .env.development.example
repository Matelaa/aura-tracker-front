# Copy to .env.development (gitignored) for local development.

# Where aura-back is running locally. Read client-side too (the <model-viewer>
# element fetches models directly from here) — must be NEXT_PUBLIC_ to be available
# in the browser bundle.
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000

BEHIND_TLS_PROXY=false
