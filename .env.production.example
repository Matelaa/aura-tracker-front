# Copy to .env.production (gitignored). Fill in the real deployed backend URL before
# any real deploy — this default is only correct for local testing.

NEXT_PUBLIC_BACKEND_URL=http://localhost:3000

BEHIND_TLS_PROXY=true
